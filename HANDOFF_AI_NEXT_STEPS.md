# HANDOFF - Dự án website AQURON 2000 / Bê tông

File bàn giao chính nằm tại:

```text
D:\web betong\HANDOFF_AI_NEXT_STEPS.md
```

Đọc file đó trước khi làm tiếp dự án.

Tóm tắt nhanh:

- Website chính: `D:\web betong\demo-site`
- GitHub: `https://github.com/tuanhp88fx/aquron-betong-demo.git`
- Branch: `main`
- Local preview: `http://127.0.0.1:3008`
- File chính sửa giao diện: `app/page.tsx`
- Build nên dùng:

```powershell
$env:NODE_OPTIONS='--max-old-space-size=4096'; npm run build
```

Nguyên tắc quan trọng: nội dung public gửi đối tác phải chuyên nghiệp, không để lộ chữ nội bộ như `demo`, `upload`, `thư mục`, `vidieo`, `D:\web betong`, `Tuấn`.
